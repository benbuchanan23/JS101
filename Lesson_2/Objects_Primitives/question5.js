let myWords = ['Hello'];
myWords[0].toUpperCase();    // returns ['HELLO'], but if not assigned a variable, the original string is not mutated within the array

console.log(myWords);    // prints ['Hello']